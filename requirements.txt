# Python 3.12.13 and PyTorch 2.7.1 are fixed; the CUDA build of PyTorch is free.
# Install torch first from the wheel index matching the card, e.g.
#   pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu126   # or cu121 / cu124
# then `pip install -r requirements.txt` for the rest. The full `pip freeze` of the two
# machines the cells ran on is in env/.
torch==2.7.1
#
# --- the receivers, probes, baselines (tools/, models/) ---
transformers==4.52.3
tokenizers==0.21.4
safetensors==0.7.0
huggingface_hub==0.36.2
numpy==2.3.5
scipy==1.17.1
scikit-learn==1.8.0
datasets==4.7.0
lm_eval==0.4.11
#
# --- optional: models/selfExtend flash-attention support only; the pipeline runs eager / sdpa ---
# triton (3.1.0 on the cluster, 3.3.1 on the dev host), flash_attn (2.8.3 / 2.8.2, built wheels)
