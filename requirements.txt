# selfextendPCW -- the two environments behind the 2026-09 results.
#
#   the cluster (A100 40 GB): every server job in RESULTS 60-63 -- torch 2.5.0+cu121,
#         cuDNN 9.1.0.70, triton 3.1.0, flash_attn 2.8.3. Full freeze: env/pip-freeze-cluster-2026-09-13.txt
#   the dev host (RTX A6000 48 GB): the kernel and memory measurements of RESULTS 63.17
#         and the local smoke cells -- torch 2.7.1+cu126, triton 3.3.1, flash_attn 2.8.2.
#         Full freeze: env/pip-freeze-devhost-2026-09-13.txt
#
# Everything else the code imports is pinned to the SAME version on both machines (below).
# Python 3.12.13 (conda-forge) on both. Install torch first from the index matching the card's
# CUDA, e.g.  pip install torch==2.5.0 --index-url https://download.pytorch.org/whl/cu121
torch>=2.5.0,<2.8
#
# --- the receivers, probes, baselines and the bench (tools/, bench/, models/) ---
transformers==4.52.3
tokenizers==0.21.4
safetensors==0.7.0
huggingface_hub==0.36.2
numpy==2.3.5
scipy==1.17.1
scikit-learn==1.8.0
datasets==4.7.0
lm_eval==0.4.11
#
# --- optional: models/selfExtend flash-attention support only; the pipeline runs eager / sdpa ---
# triton (3.1.0 on the cluster, 3.3.1 on the dev host), flash_attn (2.8.3 / 2.8.2, built wheels)
