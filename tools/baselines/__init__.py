"""H6 external baseline runners (prereg section 9, baseline_under_review.md).

Everything a runner is allowed to read goes through `common.py`. See its
docstring for why that is a structural rule rather than a convention.
"""
